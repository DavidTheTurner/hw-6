\question{(B3.1)}{
    Use the chain rule to prove that
    \[
        d\,\mathrm{det}_I(B) = (\det\circ\, \gamma)'(0),
    \]
    where $d\,\mathrm{det}_I$ is the derivative of the determinant function
    $\mapdef{\mathrm{det}}{\mathrm{M}_n(\reals)}{\reals}$ at $I$
    (the identity matrix).
}
