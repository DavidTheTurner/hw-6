\question{(B2.1)}{
    If $a^2 + bc < 0$, let $\omega > 0$ be the real such that
    $\omega^2 = -(a^2 + bc)$.
    Prove that
    \[
        e^A = \cos \omega I + \frac{\sin \omega}{\omega} A.
    \]
}
